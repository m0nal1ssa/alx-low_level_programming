#include "main.h"
#include <stdio.h>

/**
  * _abs - computes the absolute value
  * @num: is the integer to compute
  * Return: 0
*/

int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
